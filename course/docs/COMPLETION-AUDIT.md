# 科学日麻牌效率课程完成审计

## 目标与证据

### 完整教学

- `index.html` 提供六阶段、十八课的完整学习路径。
- `lessons/0001-*.html` 至 `lessons/0018-*.html` 每课包含单一技能目标、图像例题、三道即时反馈题、来源、前后导航和追问提醒。
- 总计五十四道课程检索题；课程覆盖向听、有效牌、剩余枚数、搭子、浮牌、五牌块、六牌块、复合形、对子、一向听三类型、复杂听牌、完整词典序和速度训练。

### 牌面直观

- `assets/tiles/` 包含三十四种本地 SVG 日麻牌与 `manifest.json`。
- 所有具体牌例使用矢量图片和中文替代文本；教学正文不显示紧凑字母牌谱。
- 舍牌、进张与已知牌分别使用中文标记、颜色和划除线表达。

### 科学与可计算

- `lib/mahjong.mjs` 实现普通四面子一雀头向听、有效牌、剩余枚数和舍牌分析。
- `reference/probability-model.html` 给出下一摸命中概率、有限巡目组合模型、两步改良代理量及不存在万能分数的原因。
- `reference/decision-model.html` 将舍牌比较固定为：向听数、直接剩余枚数、进张质量、二次改良、最终形。
- `tests/mahjong-engine.test.mjs` 锁定完成形、听牌、一向听、边张、重复牌、三类一向听与三种复杂听牌。

### 形成实战速度

- `trainer.html` 使用二十个基础结构，经六种花色置换与镜像生成二百四十个同构变体。
- `lib/training.mjs` 让不同基础结构交错出现，同一结构隔二十题再测试，避免连续同型造成虚假熟练。
- 每题记录作答时间、正确与否、向听错误或枚数错误，并保存最近五百次记录。
- `mastery.html` 要求同时通过十八课、至少一百手、近百手正确率九成、近五十手中位时间五秒四道门。

### 范围忠实

- 课程只研究普通四面子一雀头的进攻牌效率。
- 明确排除防守、点棒顺位、七对子、国士无双与复杂打点权衡，符合当前学习阶段。
- 没有提前创建学习记录或声称学习者已经掌握；实际掌握由课程答题与训练数据证明。

## 自动验证

```powershell
node --test tests/training.test.mjs tests/course-completeness.test.mjs tests/mahjong-engine.test.mjs
node tests/lesson-0001-smoke.mjs
git diff --check
```

结果：

- 十八项 Node 测试通过。
- 第一课兼容烟雾测试通过。
- 三十四张牌、十八节课程、五份速查、三项训练工具和全部本地链接均完整。
- 二百四十个训练变体全部可解析为合法十四张手牌。
- 工作树格式检查无错误。

## 浏览器验证

- 桌面课程首页显示十八张课程卡与三个训练工具入口。
- 第一课错误反馈、正确反馈与刷新后进度保存正常。
- 视觉分析器可载入十四张示例并输出十三种舍牌排名。
- 训练器可提交舍牌、显示精确反馈、持久化训练统计并同步到掌握仪表板。
- 首页、高级课程、训练器与仪表板在 390 像素视口均无页面级横向溢出。
- 浏览器控制台无错误或警告。

