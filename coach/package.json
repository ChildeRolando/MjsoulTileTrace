{
  "name": "riichi-coach",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w @riichi-coach/contracts && npm run build -w @riichi-coach/reasoning",
    "test": "npm run build && vitest run",
    "build:fact-engine": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-fact-engine.ps1",
    "generate:factor-regression-golden": "node scripts/generate-factor-regression-golden.mjs",
    "test:fact-engine": "cd tools/mahjong-facts && go test ./...",
    "test:package-import": "npm run build && node --test smoke/package-import-smoke.mjs",
    "typecheck": "tsc --noEmit -p packages/contracts/tsconfig.json && tsc --noEmit -p packages/reasoning/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "typescript": "5.9.2",
    "vitest": "3.2.7"
  }
}
