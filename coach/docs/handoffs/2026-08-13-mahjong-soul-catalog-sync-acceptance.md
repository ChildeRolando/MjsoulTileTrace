# M5-C 雀魂目录同步验收交接

日期：2026-08-13

## 验收范围

- 待验收分支：`prototype/m5c-catalog-sync`
- 待验收提交：`ea4fd72`
- 基线：`master` / `99b5bde`
- 集成分支：`codex/m5c-catalog-sync-integration`

本次没有原样合并待验收分支。已择取 M5-C 的安全目录同步核心，并排除后续 M5-E 实验性网络接线：该实验路径允许未受信网关、可能向错误主机发送账号令牌、记录上游原文，而且实际会话恢复仍失败。

## 已接受并加固的能力

- 国区雀魂分享链接与安全目录 DTO。
- 受限 Lobby RPC、分页目录同步、详情精确关联。
- 账号绑定的 AES-GCM 本地目录；权威替换，最多保留最近 30 场。
- 最近 30 场按逐步扩大的完整时间窗口获取，不把“最近 30 天”冒充“最近 30 场”。
- 只显示可分析条目：当前仅接受四人南风、固定官方记录版本与规则组合；三麻、东风、自定义规则和证据不完整条目均关闭失败。
- 注销先暂停并排空同步，再清除浏览器状态、缓存、目录和令牌；同步结果不能在注销后回写。
- 登录、初始化和注销使用同一串行边界，注销期间不能启动新的登录并在退出后复活会话。
- RPC 超时立即拒绝调用，并关闭 Lobby、编解码器及传输，避免相关表永久占用。
- 离线状态只显示本地缓存可能过期的提示，不允许发起同步。
- 协议兼容报告升级到 v3，绑定目录、详情、玩家、分页、错误和规则模式的运行时关键字段。

## 证据与限制

- `mode = 2` 的四人东南场语义来自独立、固定提交的 Akagi 解码器证据：`8a96bb5faa672ffb04946a31068c18cb0bebd63a`，文件 `src/history/aggregator.rs`。
- 已固定的官方协议帧正证 `record version = 210715` 与 `standard_rule = 2`；尚无真人详情帧进入仓库。
- 所有测试凭据、账号和令牌均为虚构值。
- 当前生产 Lobby 会话工厂仍是固定失败占位，界面默认隐藏“同步”入口。因此这次合并交付的是经过验证的目录同步内核与本地缓存，而不是已经可用的真人账号联网同步。
- 在实现安全的 M5-E 登录恢复、国区网关发现与传输前，不得打开同步入口，也不得接受任意服务端返回的网关地址。

## 验收门禁

- TypeScript 类型检查：通过。
- 全量测试：90 个测试文件，951 项通过。
- 协议更新器：26/26 通过。
- 协议兼容测试：4/4 通过。
- 独立只读复审：Critical 0 / Important 0 / Minor 0，Ready。

## 下一步

可以继续 M5-D 的完整牌谱下载、解码与可分析性管线；M5-E 的真实登录会话和网络传输应保持为独立安全切片，在完成端点白名单、令牌生命周期、真实账号恢复与人工冒烟测试后再开放同步入口。
