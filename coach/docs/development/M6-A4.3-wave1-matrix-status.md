# M6-A4.3 wave-1 验收矩阵状态（2026-08-18，已收口）

`response window × actual action` 覆盖矩阵的真实语料取证状态。数据来源：
8 份真实 Mortal 报告（雀魂 H2 1 份 + 天凤 7 份）经共享验收核心全链 E2E，
加上天凤语料纯事件 discovery 扫描（`scripts/response-surface-discovery.mjs`）。

## wave-1 六分支（全部 ✅ 已取证）

| 分支 | 真实 E2E 样本 | 来源 |
|---|---|---|
| `resp_chi_actual` | 6 | 天凤 02720fd / 8ffcebe / d91fdd / f91cc7 / d4710aa5 / 28b28381 |
| `resp_pon_actual` | 7 | 雀魂 H2 + 天凤 6 |
| `resp_daiminkan_actual` | 1 | 天凤 d4710aa5e1eefcd7#1（2026080803gm-00a9-0000-e96730d7） |
| `resp_hora_actual` | 5 | 雀魂 H2 + 天凤 4 |
| `resp_pass_on_discard` | 8 | 雀魂 H2 + 天凤 7 |
| `resp_chankan_actual` | 1 | 天凤 28b283816b231418#1（2026080804gm-00a9-0000-71cfcabf） |

### `resp_pass_on_discard` 候选族子覆盖（四族全部 ✅）

| 候选族 | 覆盖样本 | 状态 |
|---|---|---|
| chi 候选族 | 全部 8 样本 | ✅ |
| pon 候选族 | 全部 8 样本 | ✅ |
| daiminkan 候选族 | 雀魂 H2、天凤 d91fdd、f7138da | ✅ |
| hora 候选族（能荣而过） | 天凤 28b28381（chankan 局 pass 窗口） | ✅ |

hora 候选族已由真实语料取证 → 降级条款不再适用。

## wave-2（出本里程碑验收范围，fail-closed + 降级条款）

- `resp_pass_on_kakan`：fail-closed，降级条款已冻结（N=10,000）。
- 国士抢暗杠：雀魂规则存在性已确认（雀魂允许国士無双抢暗杠，wiki 实证），
  保持 wave-2 fail-closed + 降级条款。

## 发现产物（2026-08-18 全语料跑分）

`scripts/response-surface-discovery.mjs` 双来源全量扫描：
- 合格局 2,379（天凤 2,378 + 雀魂 H2 1），口径 = mapper 接受 + canonical 重放成功的四人南风局；
- `resp_chi_actual` 19,058 命中 / `resp_pon_actual` 27,424 / `resp_daiminkan_actual` 100 / `resp_hora_actual` 12,517 / `resp_chankan_actual` 3；
- 全部分支均有具体 (gameId, seat, decisionEventRef) 候选，无 uncovered 分支。

## 产物清单

- `scripts/response-surface-discovery.mjs`（天凤 + 雀魂双来源，纯事件扫描，
  chankan 最早启动，合格局计数按 source 记入 manifest）。
- reasoning `response-surface-discovery.ts`：来源无关的响应面 census。
- evidence manifest 的 `resp_pass_on_discard` record 携带
  `responsePassFamilies`（chi/pon/daiminkan/hora 子覆盖）。
